# JSON to CSV Converter Dependencies
# Python 3.8+ required

# Core dependencies (using built-in modules for now)
# No external dependencies required for basic functionality

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=5.0.0
mypy>=1.0.0

# Optional: For enhanced JSON processing
ujson>=5.0.0  # Faster JSON parsing (optional) 